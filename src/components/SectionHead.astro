---
/* Section header: mono index in the spine rail, title and lede beside it. */
interface Props { idx: string; title: string }
const { idx, title } = Astro.props;
---
<header class="shead" data-in>
  <p class="meta shead__idx">{idx}</p>
  <h2 class="h2">{title}</h2>
  <slot />
</header>
